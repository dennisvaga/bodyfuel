/**
 * Global UI-related type definitions that are shared across the application
 */

/**
 * Application variant used to determine styling and behavior in components
 */
export type AppVariant = "admin" | "shop"; 